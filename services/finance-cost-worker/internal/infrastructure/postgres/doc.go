// Package postgres will hold the worker's PostgreSQL repository
// implementations (read-only views over cost_calc_chunk + write of
// cost_calc_result rows). Concrete repos land in S8c.7.
package postgres
