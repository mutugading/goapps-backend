// Package costbulkimport provides bulk export and import-template generation for
// costing product master + routing data, plus the shared Excel error-report
// generator consumed by the v2 ETL import pipeline.
package costbulkimport

// boolTrueStr is the canonical string representation of a true boolean value in import sheets.
const boolTrueStr = "true"
