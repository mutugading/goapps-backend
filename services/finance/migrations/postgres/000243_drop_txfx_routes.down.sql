-- 000243.down: No-op. Routes are re-seeded by 000244 (which has its own down).
-- A true reversal would require rebuilding the 000239 fixture, which is
-- replaced wholesale; we don't support that.
SELECT 1;
