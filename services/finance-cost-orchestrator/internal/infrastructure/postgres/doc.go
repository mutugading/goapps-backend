// Package postgres will hold the orchestrator's PostgreSQL repository
// implementations (cost_calc_job, cost_calc_job_product, cost_calc_chunk).
// Concrete repos land in S8c.5 alongside the planner/coordinator logic.
package postgres
