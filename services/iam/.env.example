# =============================================================================
# IAM Service - Environment Variables
# These override values in config.yaml via Viper's env binding.
# For local dev, config.yaml defaults are usually sufficient.
# Copy to .env and adjust only what you need to override.
# =============================================================================

# --- App ---
# APP_ENV=development                    # development | staging | production
# LOG_LEVEL=debug                        # debug | info | warn | error

# --- Database (PostgreSQL) ---
# DATABASE_HOST=localhost
# DATABASE_PORT=5435
# DATABASE_USER=iam
# DATABASE_PASSWORD=iam123
# DATABASE_NAME=iam_db
# DATABASE_SSLMODE=disable

# --- Redis (sessions, blacklist, OTP, rate limit - DB 1) ---
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=

# --- JWT ---
# JWT_ACCESS_SECRET=dev-access-secret-change-in-production
# JWT_REFRESH_SECRET=dev-refresh-secret-change-in-production

# --- Email / SMTP ---
# Local dev: Mailpit (docker compose) on port 1025, Web UI at http://localhost:8025
# Production: Use Mailtrap, SendGrid, SES, etc.
# SMTP_HOST=localhost
# SMTP_PORT=1025
# SMTP_USER=
# SMTP_PASSWORD=
# SMTP_FROM_ADDRESS=noreply@goapps.local
# SMTP_FROM_NAME=GoApps IAM
# SMTP_USE_TLS=false
# SMTP_SKIP_VERIFY=true

# --- CORS ---
# CORS_ALLOWED_ORIGINS=http://localhost:3000

# --- Tracing (Jaeger/OpenTelemetry) ---
# TRACING_ENABLED=true
# JAEGER_ENDPOINT=localhost:4317
